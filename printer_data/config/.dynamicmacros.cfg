[gcode_macro MYTEST]
gcode = 
	DYNAMIC_MACRO MACRO=MYTEST {rawparams}

[gcode_macro HEAT_HOTEND]
gcode = {% set temp = params.TEMP|int %}
	DYNAMIC_MACRO MACRO=HEAT_HOTEND {rawparams}

[gcode_macro CHECK_PROBE]
gcode = 
	DYNAMIC_MACRO MACRO=CHECK_PROBE {rawparams}

